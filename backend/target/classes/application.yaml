spring:
  application:
    name: shortenly
  datasource:
    url: jdbc:postgresql://localhost:5432/shortenlydb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  base-url: http://localhost:8080/
  frontend-url: http://localhost:4200/
